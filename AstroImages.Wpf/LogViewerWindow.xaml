<Window x:Class="AstroImages.Wpf.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Log content -->
        <TextBox x:Name="LogTextBox" 
                 Grid.Row="0"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Padding="5"
                 TextWrapping="NoWrap"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Refresh" Width="80" Margin="5" Click="RefreshButton_Click"/>
            <Button Content="Clear Log" Width="80" Margin="5" Click="ClearButton_Click"/>
            <Button Content="Copy to Clipboard" Width="120" Margin="5" Click="CopyButton_Click"/>
            <Button Content="Close" Width="80" Margin="5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
